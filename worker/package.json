{
    "name": "recipe-worker",
    "version": "1.0.0",
    "description": "Cloudflare Worker for Recipe API",
    "main": "index.js",
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy"
    },
    "dependencies": {
        "hono": "^4.0.0",
        "postgres": "^3.4.7"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}
